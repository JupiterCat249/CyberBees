# V-004-3 迭代004 · 引擎接线 + 默认 Pattern · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：**迭代004 · 第3步（接线）· 第4步（默认 Pattern）**
> 封存日期：2026-08-27

## 第3步：接线

| 文件 | 改动 |
|------|------|
| `battle_state.gd` | 新增 `var anim = null` |
| `battle_flow.gd` | 注入：`load(battle_anim.gd)` → `new()` → **`state.add_child(an)`**（Node 需挂场景树才能逐帧推进）→ `register_defaults()` → `state.anim = an`；**启动接线自检 16 → 17 项** |

## 第4步：默认 Pattern（7 条，依据表现规范）

| Pattern | Unit 类型 | 内容 |
|---------|-----------|------|
| `受击抖动` | MOVE_BY（可叠加） | 左右往复 6px×2 帧 → 反向抵消回原位（单位受击）|
| `受伤闪红` | TINT | `#FF2000` → `#FF8800` → 白（各 2 帧）|
| `地图抖动` | MOVE_BY（可叠加） | 上下左右 12px 往复 + 收尾半幅（**AOE 地图抖动**）|
| `卡牌登场` | TINT | **瞬间动作（无前摇）**：第 1 帧即到位，仅淡入 4 帧 |
| `卡牌退场` | TINT | 偏红 → 透明（末帧消失，无后摇）|
| `回合色-我方` / `回合色-敌方` | TINT | `#499169` / `#A84331` |

**数值文本规范**（供文本层调用）：
- 颜色常量：`DAMAGE_COLOR = #ff2000` · `REFUND_COLOR = #ffa300` · `HEAL_COLOR = #00dd00`
- `text_size_for(v)`：**字号随数值**（≥8→72 / ≥5→60 / ≥3→52 / 其它 44）

## 验证（实机 eval）

| 项 | 结果 |
|----|------|
| `state.anim` 注入 | `true` ✅ |
| 已注册 Pattern | **7 条**：受击抖动 / 受伤闪红 / 地图抖动 / 卡牌登场 / 卡牌退场 / 回合色-我方 / 回合色-敌方 ✅ |
| 颜色常量 | `ff2000` / `ffa300` / `00dd00` / `499169` / `a84331` —— 与表现规范**逐项一致** ✅ |
| 字号随数值 | `2→44 · 4→52 · 6→60 · 9→72` ✅ |
| 受击抖动实测 | ⏳ 未跑起：测试用 state API 直接 `spawn`，BoardView 尚未建对应节点 → 下次走**正常部署路径/真实鼠标**补测 |

## 待完成（迭代004 后续）

| 步 | 内容 |
|----|------|
| 4b | **接入点**：把 Pattern 挂到实际表现时机 —— 受击（`attack_resolved` / `unit_hp_changed`）· AOE（溅射技能）· 卡牌登场退场（手牌/部署/墓地）· 回合色（`turn_started`）|
| 5 | 数值文本落地：`#FFA300` 回费 / `#FF2000` 受伤 / `#00DD00` 回血 + **字号随数值**；六个瞬间动作无前摇 |
| 6 | UI 回合色实际应用（我方/敌方/#FFFFFF-50%）|
| 7 | 实机验证（含真实鼠标）+ T13 封存 + 系统维护建档（基础描述/API/**扩展：如何新增动画**）+ 迭代004 验收 + tag |

## 备注（工程纪律）

本轮 `battle_state.gd` 的 `var anim` 一行由 **bash/python 直改**写入（未走 MCP），
已即时以 `filesystem_manage(scan)`/重启验证落盘生效；**后续脚本改动一律回归 MCP**（遵守 T12）。
