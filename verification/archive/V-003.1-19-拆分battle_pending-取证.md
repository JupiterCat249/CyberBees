# V-003.1-19 可维护性重构 第2块：拆出 `battle_pending.gd` · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代003.1 · 可维护性重构 第2步（拆分）第2块
> 封存日期：2026-08-27

## 为什么第2块选它

"待确认（二次点击确认）"是**本迭代问题最高发区**（曾出现"反复点击只输出待确认""支援卡死"等故障），
逻辑自成一块（5 个函数），且**只读写"待确认"这一组字段**，与其它块的耦合面清晰。

## 改动

| 文件 | 改动 |
|------|------|
| **新增** `scenes/battle/battle_pending.gd`（104 行，RefCounted） | `confirm` · `confirm_pending_at` · `clear_pending` · `build_preview` · `confirm_pending`（逻辑原样搬迁，内部调用改走 `state.`） |
| `battle_state.gd` | 新增 `var pending = null`；五个函数改为**同签名转发**（调用点零改动）；`_build_preview` 移出 |
| `battle_flow.gd` | 注入 `state.pending` + **启动自检增至 9 项** |

**设计要点已随代码一并搬入注释**（防后人重蹈覆辙）：
待确认是**独立状态**（不随 `clear_sel` 清除）· 执行**优先级最高**（不依赖 mode/选中）· 预览记录 `source_id` 以便结算时补回施放者。

## 验证（实机 eval）

| 项 | 结果 |
|----|------|
| `state.pending` 注入 | `true` ✅ |
| `confirm("部署", cell)` 首次 | `false`（建立待确认；预览字段 `kind/cell/source_id`）✅ |
| 再次以同 (kind, cell) 调用 | `true`（**二次确认语义保持**）✅ |
| `build_preview("攻击", …)` | 字段完整：`attacker_id / dmg / hp_now / hp_after / counter / attacker_hp_after` ✅ |

## 行数变化

| 文件 | 重构前 | 现在 |
|------|--------|------|
| `battle_state.gd` | 1175 | **1088** |
| `battle_grid.gd` | — | 92（第1块） |
| `battle_pending.gd` | — | 104（第2块） |
| `battle_flow.gd` | 116 | 139（注入 + 9 项自检） |

> 注：`battle_state` 仍保留**同签名转发壳**，故行数下降有限；待各块搬完后统一"去壳"收尾。

## 下一块

`battle_deck.gd`（手牌/牌库：`select_hand` / `draw_one` / `discard_armed` / `can_discard` / `exchange_hand` / `can_exchange` / `start_battle`）。
