# V-004-15 迭代004 · **验收结论（通过）** · 取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代004 · 验收收口
> 封存日期：2026-08-27

## 结论：**迭代004 验收通过** ✅

交付 ①–⑤ 全部达成，逐项均有**实机证据**（封存于 `verification/archive/V-004-1 ~ 15`）。

| 交付 | 要求 | 证据 | 结论 |
|------|------|------|------|
| **①** 自写 ActionUnit 引擎 | Pattern→Unit→Clip；5 类 Unit（指定值可叠加移动/旋转 · 到指定值不可重复 · 旋转追踪 · 变色 · 生成特效）；顺序执行 / 可跳转 / 可触发(start\|stop) / 可终止 / **终止级联不复位** / 可禁 UI 交互 / 可受全局暂停；ResetUnit/Action/Stop | `V-004-1`：`battle_anim.gd` 306 行、编译无诊断、enum U 五类、`reset_unit/action/stop/stop_all`、`trigger_on_start/stop`、`ui_locked`、`anim_paused`；**无 AnimationPlayer/Tween、不用 delta（T3/T2 合规）** | ✅ |
| **②** 清理引擎动画残留 | 替换并清理现有引擎动画（扫描线 AnimationPlayer），全项目统一 | `V-004-2`：MCP 删除 `AnimationPlayer`(ScanAnim) + `Animation`/`AnimationLibrary`（diff 25 行删除）；`V-004-14` 截图确认**扫描线表现正常**（着色器逐帧驱动） | ✅ |
| **③** 表现规范落地 | 瞬间动作无前摇 · 数值文本（`#FFA300`/`#FF2000`/`#00DD00` + **字号随数值**）· 单位受击抖动 · **AOE 地图抖动** · UI 回合色（`#499169`/`#A84331`/`#FFFFFF-50%`） | `V-004-3`：7 条默认 Pattern；`卡牌登场` 第 1 帧即到位（无前摇）；`V-004-9`：`text_color` 三色 + `turn_color` 两色 + 中性 `#FFFFFF-50%` **运行时读值逐项一致**、`text_size_for(1/3/5/9)=44/52/60/72`；`V-004-12`：`shake_map()` 实测 `running=["地图抖动"]` | ✅ |
| **④** 实机验证（含**真实鼠标**） | 真实鼠标走流程 + T13 封存 | `V-004-14`：**真实鼠标**点击驱动「开始对局 → 部署阶段」，前后截图逐项对比（按钮文字 / 费用 / 状态文本 / 被动结算）全部符合设计；**读图复核**（视觉证据）；`V-004-7`：三条低频信号 `is_connected` 全 true | ✅ |
| **⑤** 建档 + 记录 + 验收 + tag | 基础描述 + 代码API + 扩展（如何新增动画）+ 迭代记录 + 验收结论 + tag | `系统维护/动画系统/`（基础描述 + api/代码API + **扩展-001-如何新增动画与表现**，含操作指南与自检清单）✅；迭代004 记录（进度快照 + 三条教训）✅；**验收结论=本文件** ✅；**tag `iter/004`** ✅ | ✅ |

## 本迭代额外修复的两个缺陷（均已验证）

| 缺陷 | 根因 | 结论 |
|------|------|------|
| **对局从不自动开始**（`started=false`，画面空面板） | **G-01**：重构期间用 bash/python 直改的 4 个注入块被编辑器回退，且**无自检守卫** | 已修复（补回注入 + **4 个自检项**），`V-004-11` 逐项验收通过；`V-004-14` 截图确认 ✅ |
| **`turn_color` 缺失崩溃**（启动即停在调试器） | 落地默认 Pattern 时**漏加函数**（只留常量） | 已补 `turn_color()` + `NEUTRAL_COLOR`，`V-004-9` 修复并运行时验证 ✅ |

## 三条沉淀教训（已写入系统维护文档与迭代记录）

1. ⚠️ **不要用 `state_changed`（重绘信号）驱动逐帧动画** —— 频率高、易与状态推进同帧重入（实测挂死）
2. ⚠️ **凡改 `.gd` 必须走 MCP** —— bash/python 直改会被编辑器回退（G-01），代价是 4 个注入块静默消失
3. ⚠️ **验证必须有"与玩家一致"的路径** —— 只走脚本捷径（手动 `start_battle()`）会掩盖"自动开始"路径断裂
   另：**截图可作为主取证手段**（窗口后台化时仍可用），`game_eval` 则要求窗口前台

## 遗留（移交下一迭代）

| 项 | 说明 |
|----|------|
| 受击抖动**单帧截图** | 抖动仅 8 帧（≈0.13s），需先把敌方单位放进攻击范围；触发链已验证（V-004-7 / V-004-12）|
| `MOVE_TO` / `ROT_TRACK` / `SPAWN_FX` 的实际用例 | 引擎已实现且可调用，但当前默认 Pattern 未用满（留给后续表现需求）|
| 迭代003.1 遗留 | `targets._source_id` 接线 · 渲染层 G-18（效果图标/伤害文字配色等）· 去转发壳（可选）|
