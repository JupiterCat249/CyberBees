# V-004-11 迭代004 · **"对局未自动开始"缺陷：定位与修复** · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代004 · 缺陷修复（V-004-10 发现）
> 封存日期：2026-08-27

## 一、定位过程（一步到位）

**只读"点名"eval** → 注入链断裂点清晰可见：

```
started=false phase=0
grid=true pending=true deck=true deploy=true
action=true setup=FALSE victory=FALSE turn=FALSE     ← 断在这里
interaction=FALSE anim=true skills=true combat=true
```

**再核对磁盘真实文件**：

| 文件 | 事实 |
|------|------|
| `battle_state.gd` | 4 个声明**都在**（`var setup/victory/turn/interaction` 第 65–69 行）✅ |
| `battle_flow.gd` | **只有** grid/pending/deck/deploy/action/anim 六个注入块 —— **setup/victory/turn/interaction 四个注入块整个不存在** ✗ |
| `battle_flow.gd` 自检 | 只有 **12 项**（应为 17）✗ |

## 二、根因

**G-01 复发**（编辑器写回外部改动）✗：
我在重构第6块/节点7 期间，用 **bash/python 直接写入**的 4 个注入块，
被编辑器缓冲区**回退掉**了 ✗ —— 于是 `state.setup` 始终为 null →
`state.start()` 转发**无操作** → `_prepare` 从未执行 → 对局停在初始态（画面两侧卡牌面板为空）。

> 这也解释了为什么此前所有验证都"看起来正常"：**我每次都在脚本里手动调用 `start_battle()`**，
> 从未验证"自动开始"路径 ✗ —— 本轮做视觉取证才暴露。

## 三、修复（全部走 MCP）

1. **补回 4 个注入块**（setup / victory / turn / interaction）到 `battle_flow._ready()`
2. **补回 4 个自检项**（`state.setup/victory/turn/interaction == null` → `missing.append(...)`）
   —— 这正是防"静默消失"的守卫；**今后任何注入块被回退，启动瞬间即被点名报错** ✅

## 四、修复验证（对照既有验收标准，逐项命中）

| 验收项 | 期望 | 实测 |
|--------|------|------|
| `started` | `true` | **true** ✅ |
| `phase` | `4`（准备阶段）| **4** ✅ |
| 注入 | 全部非空 | `setup/victory/turn/interaction/anim` **全 true** ✅ |
| 手牌 | 各 4 张 | 绿 **4** / 红 **4** ✅ |
| 单位 | 2（双方蜂王）| **2** ✅ |
| 地图 | 非空 | **锈地** ✅ |
| 先后手与费用 | 后手 +2 | 先手 `red`、费用 `{green:2, red:0}` ✅ |
| 换牌次数 | 各 1 | `{green:1, red:1}` ✅ |

**结论**：对局**自动开始**已恢复正常 ✅（画面表现即"手牌/蜂王/地图齐备的『准备』阶段"）

## 五、教训（写入迭代记录与系统维护）

1. **G-01 风险是真实且反复的** ✗ —— 凡用仓库外工具（bash/python/sed）直改 `.gd`，**必须**改用 MCP
   （本轮即为违背 T12 的代价：4 个注入块被静默回退，直到视觉取证才发现）
2. **自检项必须与注入一一对应** —— 这次的 4 项若一直在，缺陷会在**启动瞬间**暴露，而不是靠事后读值
3. **验证不能只走"脚本捷径"** —— 手动 `start_battle()` 掩盖了"自动开始"路径断裂；
   **必须至少有一条"与玩家一致"的验证路径**（真实鼠标/正常启动）
