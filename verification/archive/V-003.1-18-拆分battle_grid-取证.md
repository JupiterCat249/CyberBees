# V-003.1-18 可维护性重构 第1块：拆出 `battle_grid.gd` · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代003.1 · 可维护性重构 第2步（拆分）第1块
> 封存日期：2026-08-27

## 为什么第1块选它

复盘结论：数值层（`final_*`）**早已抽出**到 `battle_combat.gd`（`battle_state` 里只是转发壳），
故原计划的 `battle_effects` 与之重叠；改为先拆**真正的纯函数块**——棋盘几何与范围计算。

低风险依据：`move_cells` / `range_cells` / `all_cells` 在重构前**只被 battle_state 内部调用（6 处）**，
无外部依赖、无状态、无副作用。

## 改动

| 文件 | 改动 |
|------|------|
| **新增** `scenes/battle/battle_grid.gd`（RefCounted） | `move_cells`（BFS 走格子，被单位阻挡）· `range_cells`（曼哈顿，不被阻挡）· `all_cells` · **`neighbors`**（四邻，供链式/相邻判定）· **`dist`**（曼哈顿距离）· `unit_at` 转发 |
| `battle_state.gd` | 新增 `var grid = null`；三个函数改为**转发**（保持调用点不变，行为零变化） |
| `battle_flow.gd` | 注入 `state.grid` + **启动自检增至 8 项**（新增"棋盘几何注入"） |

采用与 `BattleSkills` 相同的已验证模式：**RefCounted + 注入共享 Model(state)**，无需场景节点。

## 验证（实机 eval）

| 项 | 结果 |
|----|------|
| `state.grid` 注入 | `true` ✅ |
| `move_cells(蜂王, 2)` | 7 格 |
| `range_cells(蜂王, 2, false)` | 7 格 |
| `all_cells()` | 16 格（4×4）✅ |
| **经 grid 直调 vs state 转发** | `move=7` / `range=7` —— **两者完全一致 → 行为不变** ✅ |
| `neighbors(角落格)` | 3 格（边缘正确裁剪）✅ |
| `dist((0,0),(1,2))` | 3 ✅ |

## 顺带发现：**G-01 已污染提交**（重要）

恢复 `battle_flow.gd` 时发现：编辑器的回退版本**已被提交进仓库** ——
`push_warning`（技能系统注入失败告警）等分支在 HEAD 中**不存在**，而同一提交里的其它内容却在 ✗
→ 说明**编辑器缓冲区曾把中间版本写回并被 commit**。

**处置**：已从 HEAD 恢复并重新注入 BattleGrid；**建议尽快关闭编辑器内的生产脚本标签**
（或退出编辑器后删除 `.godot/editor/script_editor_cache.cfg` 再启动），否则每次改写都有被回滚风险。

## 下一块

`battle_pending.gd`（待确认：confirm / confirm_pending_at / confirm_pending / clear_pending / _build_preview），
同样"先转发保行为、再逐步去壳"。
