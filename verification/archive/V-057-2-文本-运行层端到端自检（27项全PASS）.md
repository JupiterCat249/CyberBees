# 迭代057 运行层端到端自检（验证用 · 非生产）

> 状态：已封存（验证用，非生产文件）
> 对应迭代：迭代057 · C3~C6（新战斗系统接入 UI 的运行层验收）
> 封存日期：2026-09-19
> 执行场景：`verification/iter057_runtime_check.tscn`
> 方式（人要求 U-2）：**实例化真实场景 `battle_scene.tscn`（挂 arena_view.gd）**，不单独测引擎

## 一、结果：**27 项全 PASS / 失败 0**

### ① 开局（9 项）
| 项 | 结果 |
|---|---|
| 战斗场景可加载 / 可实例化 | ✅ |
| 根节点挂了 `arena_view.gd` | ✅ |
| 视图已建起 `BattleEngine` | ✅ |
| 我方蜂王在位 · **坐标 (行1,列3)** | ✅（人指定） |
| 敌方蜂王在位 · **坐标 (行2,列0)** | ✅（人指定） |
| 开局费用 先手 **4**（回费后） | ✅ |
| 开局费用 后手 **2**（后手 +2） | ✅ |
| 开局阶段 = **部署**（回费/场地已自动推进，P-1） | ✅ |

### ② 手牌容器（3 项）
我方 4 张 · 敌方 4 张（**容器装下了**）· `columns = 2`（未回退单列）✅

### ③ 部署（6 项）
选中手牌 → **拿到部署预览** → 有可选格 → 部署成功 → 场上单位 +1 → 手牌 -1 → **费用按卡费扣除（叶蜂=2）** → **单位节点已渲染到棋盘** ✅

游戏日志实证：
```
[BATTLE] 玩家·绿 部署 叶蜂 到 (2,3)（费 -2）
```

### ④ 行动阶段（3 项）
部署→行动阶段 ✅ · 选中单位拿到移动预览 ✅ ·
**刚部署的单位本回合不可移动（a500 行动机会 3）** ✅ ← 规则真实生效

### ⑤ 换手 + 补牌（2 项）
结束回合后换到红方 ✅ · 红方手牌补至 4 张（a500 抽卡 4）✅

游戏日志实证：
```
[BATTLE] 玩家·红 回合开始（第 1 回合）
[BATTLE] 玩家·红 回费 +4（现 6）
```

## 二、运行截图复核（U-3）

对 `battle_scene.tscn` 实机运行截图逐项核对：

| 复核项 | 观察到 |
|---|---|
| 手牌 2×2 | ✅ 双方各 2×2，费用 **2 / 4 / 8 / 2** 与卡池一致 |
| 蜂王位置 | ✅ 红方在上排、我方在下排，**水平错开一列**（红左、我右）→ 与 (2,0)/(1,3) 吻合 |
| 费用 | ✅ 绿 **4**（先手回费后）· 红 **2**（后手 +2） |
| 主按钮 | ✅ 显示 **完成部署**（部署阶段） |
| 阶段/回合 | ✅ **回合1--绿方** · **阶段：部署（绿）** |

## 三、蜂王起始位置修正（人 2026-09-19 指定）

人给出的坐标（右+、下+、左上原点，1-based「列,行」）：
- 红方蜂王 **(1,3)** → 0-based **行2 · 列0**
- 我方蜂王 **(4,2)** → 0-based **行1 · 列3**

**中心对称校验**（棋盘 4×4，几何中心在 (行1.5, 列1.5)）：
```
红方偏移 = (2−1.5, 0−1.5) = (+0.5, −1.5)
我方偏移 = (1−1.5, 3−1.5) = (−0.5, +1.5)   → 互为相反数 ✅ 严格中心对称
```
旧值 (3,1)/(0,1) 两王同列、各距中心 120px 而**相差 90px** → 已废弃。
两处实现已同步：`scripts/battle/battle_engine.gd` 与 `scripts/game/game_state.gd`。

## 四、遗留观察（非阻塞）

| # | 观察 | 说明 |
|---|---|---|
| O-1 | 棋盘底图未显示 | 截图里地图区域为深色纯色，地图贴图未接入（属素材接入项，不影响逻辑） |
| O-2 | 详情区仍为占位文本 | 未点选卡牌时显示"卡牌名称 / 技能描述-行1..3"；点选后由 `show_detail()` 填充（已实现，待实测） |
| O-3 | 手牌卡内图未随卡放大 | 卡内部元素仍是 200px 绝对坐标 → 卡被容器放大后**内容左对齐**（属 C1 已知取舍，人不要求处理） |
