# 迭代054 基础框架自检日志（验证用 · 非生产）

> 状态：已封存（验证用，非生产文件）
> 对应迭代检查点：迭代054 · 基础框架（规则层 + UI 接线）
> 封存日期：2026-09-17
> 执行场景：`verification/iter054_framework_check.tscn`（规则层）· `verification/iter054_e2e_check.tscn`（UI 端到端）

## 一、规则层自检：**39 项全 PASS / 失败 0**

逐条验证 `电子蜂a500规则.md`：

### 回合流程
- 开局在我方回费后**停在部署阶段**
- 主按钮：部署 → 行动阶段
- 主按钮：行动 → **换手到敌方**（回合数不变）
- 敌方行动结束 → **回合数 +1** 且回到我方

### 费用经济
- 先手开局回费 = 蜂王回费 4
- **后手初始费用 +2**
- **第 7 回合起回费 +2**（4+2=6）
- **费用上限 10**

### 部署合法性
- 兵蜂：可部署在蜂王相邻格
- 兵蜂：**不能**部署在非相邻格
- 建筑：可部署在己方领地
- 建筑：**不能**部署到敌方领地
- **费用不足不能部署**
- **部署时没有行动机会**

### 范围与移动
- 移动范围：**不含被占据格**（移动被单位阻挡）
- 移动范围：按**走格子**计算

### 战斗结算
- 预览：守方掉 3 血 / 攻方被反击 2 血（射程内）
- 主动攻击成功；**同时结算**：守方 5-3=2 且 攻方 9-2=7
- **射程外：反击无效**，攻方未掉血
- **攻击计算优先于伤害减免**：攻击力 5 − 减伤 2 = 3

### 行动机会
- 可移动 → 移动后**可再攻击**（1 移动 + 1 攻击）
- **攻击后自动结束行动**

### 效果系统
- 赋予效果成功；攻击力 3+1=4
- **T14 同名效果不叠加**（层数=1）
- **乘优先于加**：(3×2)+1 = 7
- **蜂王免疫指令伤害**
- **护盾抵挡指令伤害**
- **单位类型不匹配无法赋予效果**

### 手牌与对局
- 初始手牌 4 张；**回合结束补手牌至 4 张**
- 随机对局跑通（**无卡死/崩溃**，能推进回合）
- **退场单位已从棋盘移除**（无残留死亡单位）

## 二、UI 端到端自检：**23 项全 PASS / 失败 0**

- GameState 已挂载 · **棋盘 16 格点击区已建**
- **双方蜂王已入场**（2 个单位卡）· 位置正确（我 (3,1) / 敌 (0,1)）
- 我方手牌 4 张已渲染 · 顶栏费用徽章 = 4
- 回合文本 = `回合1--先手` · 主按钮文案 = `完成部署`
- 主按钮：部署 → 行动（文案变 `结束回合`）→ 换手到敌方
- **点击手牌 → 进入选中态 → 视图给可部署格高亮**
- **点击格子 → 成功部署（单位卡 +1）+ 扣除费用（-2）**
- **点击单位 → 选中态 → 可移动格高亮 → 点击格子成功移动 → 节点位置同步**
- 返回按钮信号已到达外层

## 三、验证中捕获并修复的缺陷

| # | 现象 | 真因 | 修法 |
|---|---|---|---|
| 1 | 蜂王不显示（单位节点缺失） | **`GameState.setup()` 在控制器连信号之前**就 place 蜂王并 emit `unit_spawned` → 该信号收不到 | `_refresh_all()` 增加**幂等** `_sync_unit_nodes()`：按棋盘补建缺失节点、移除多余节点（不依赖信号时序） |
| 2 | （自检中）建筑测试失败 | **测试数据问题**：部署兵蜂已花掉 2 费，剩余 2 < 建筑费 4 | 测试补足费用；并**新增**一条「费用不足不能部署」用例 |
| 3 | （自检中）补牌测试失败 | **测试数据问题**：测试卡组恰好用完，牌库与墓地皆空 → 框架**正确**地"无牌可补就停" | 卡组加第 5 张常规卡，使牌库有备卡 |

> 第 2/3 条为**测试数据问题**（非框架缺陷）；第 1 条为**真实框架缺陷**，已修。
