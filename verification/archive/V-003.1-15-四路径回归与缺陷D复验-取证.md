# V-003.1-15 四类组合路径回归 + 缺陷 D 复验 · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代003.1 · 检查点6 之前的引擎能力路径清障
> 封存日期：2026-08-27
> 复用套件：`verification/suites/V-suite-技能系统四路径回归.gd.txt`

## 前置（人已执行）

编辑器**重启**（清掉对本迭代早期误放在 `verification/` 下 `.gd` 文件的陈旧引用）→ 启动干净无错误。

## 验证结果（实机 eval，费用取 5 避开上限边界）

| # | 组合路径 | 结果 |
|---|----------|------|
| ① | **自身目标**（补给，条件 round≥3） | 费用 `5 -> 7`（+3 效果、−1 卡费），日志「技能「补给」生效（命中 1 处）」✅ **缺陷 D 已修复** |
| ② | **多效果**（回收：recycle + resource+2） | 费用 `5 -> 5`（−2 卡费 +2 效果），日志「泥蜂 退场」✅ **证实缺陷 E 为误判**（多效果路径正常） |
| ③ | **溅射**（巡航导弹，splash 1） | 主目标 20→16、相邻 20→16 ✅ |
| ④ | **链式**（扩散毒雾，chain） | `ok=true hits=2`，主 20→18、相邻 20→18 ✅ |

## 结论

四类组合路径（自身目标 / 多效果 / 溅射 / 链式）**全部可用**，四类在技能表中均有**常驻样例技能**，
可作为日常回归；对应套件已落盘为可复用资产。

## 教训固化

- **测试要避开边界值**：费用取 10（上限）时资源类 `+N` 被 `clampi` 截断 → 曾把正确行为误判为缺陷
- **测试代码要落盘**：内联 eval 用后即散、易重复踩坑 → 已改为 `verification/suites/*.gd.txt` 可复用套件
