# V-004-14 迭代004 · **真实鼠标流程 · 视觉取证**（交付④核心证据） · 取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代004 · 交付④「实机验证（含真实鼠标）」
> 封存日期：2026-08-27
> 取证方式：MCP `editor_screenshot(source="game")` 截图 + 主 Agent **读图复核**（T13 视觉证据）

## 场景

- 重置游戏实例（`stop()` → `project_run` → `status=live`，run_token 26）
- **真实鼠标**（MCP `input_mouse`，游戏窗口坐标 = 截图坐标 × 1.176）点击主按钮

## 证据 1：点击前 —— **准备阶段**（对局已正常自动开始）

读图确认 ✅
| 元素 | 观察 |
|------|------|
| 左手牌 | **4 张**（蜜蜂单位卡，含攻/速/血/射程数值） |
| 左详情面板 | 【支援】鼓舞 + 关键词（支援·单位效果·增益）+ 底部大图 |
| 右手 | 4 张卡 + **主按钮「开始对局」** |
| 右侧状态文本 | 「地图「蜜源」：特殊地形 2 格…」「对战准备：红方先手·绿方后手（初始费用 +2）」「牌组：初始手牌 4 张 + 备卡 4 张（a500 构筑 2）」「对战准备：可调整初始手牌（每方 1 次），点主按钮「开始对局」」 |
| 棋盘 | **双方蜂王已部署**（(0,1) 与 (3,1)，金色高亮格）+ 特殊地形高亮格 + 准星 + **扫描线** |
| 费用六边形 | **左 2 / 右 0** —— 与 a500「后手 +2」一致，**同屏可见** ✅ |
| 右下 | 四个小按钮（帮助/设置/信息/投降）|

> **这一张即证明：V-004-11 修复后"对局自动开始"完全正常**（此前该阶段是空面板）

## 证据 2：**真实鼠标点击主按钮后** —— 进入部署阶段

读图确认 ✅（与证据 1 逐项对比）
| 变化 | 观察 |
|------|------|
| **主按钮文字** | 「开始对局」→ **「进入行动」** ✅ |
| 费用六边形 | 左 2 / 右 2（**当前方回费生效**）✅ |
| 右侧状态文本 | 「对战准备结束，开始第一回合」「—— 第 1 回合 · 红方 ——」「【回费】红方 +2 → 费用 2（上限 10）」「部署范围被动生效（半径 1）」×2 ✅ |
| 棋盘/手牌/扫描线 | 保持一致，无异常 ✅ |

> **这一张证明：①真实鼠标事件链（InputRouter → 交互状态机 → BattleTurn）工作正常；
> ②【机场】被动技能（部署范围）正确结算；③回费与回合推进正确；④动画系统接线未破坏任何表现** ✅

## ④ 达成情况（对目标的比对）

| 要求 | 状态 |
|------|------|
| 实机验证 | ✅（本文件两张截图 + 读图复核）|
| **含真实鼠标** | ✅ **本轮打通**：真实鼠标点击驱动「开始对局 → 部署阶段」，画面逐项变化与设计一致 |
| 按 T13 封存 + 视觉证据 | ✅（本文件；主 Agent 已 `read_image` 等级读图复核）|
| 受击抖动的**单帧截图** | ⏳ 未取（抖动仅 8 帧 ≈0.13s，且需先把敌方单位放进攻击范围；其触发链 `combat.attack_resolved → 受击抖动+受伤闪红` 已在 V-004-7（连接已注册）与 V-004-12（`shake_map` 实测生效）验证）|

## 备注（环境态记录）

截图返回 `stale_frame: true`（游戏窗口后台化）—— **但画面为点击后的真实帧**（`frames_drawn` 从 1046 → 2850），
说明**截图通道在窗口后台化时仍可用**，可作为主要取证手段（`game_eval` 则要求窗口前台，见 V-004-6）。
