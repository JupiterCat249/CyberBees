# V-005-3/4/5 迭代005 B1/B2 · **死 autoload + 失效主场景 + 迭代001 残留脚本清理** · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代005 · 检查点3（死 autoload 移除）· 检查点4（死代码清理）· 检查点5（主场景唯一化 + 冷启动）
> 封存日期：2026-08-28

## 一、清理前的工程事实（为什么是"真隐患"而非洁癖）

| 项 | 事实 | 风险 |
|----|------|------|
| `[autoload] GameState="*res://scripts/game_state.gd"` | 该脚本是**迭代001 早期的第二套状态机**（自带 `Phase/IMode/费用/手牌/units` 与全套信号），与现行 `BattleState` **并存** | 每局都实例化一份无人使用的全局单例；后续任何"接错状态机"都会表现为**诡异的不同步**，且 `scenes/**` 全量 grep 零引用 |
| `[run] main_scene="res://scenes/battle.tscn"` | 该场景**已不存在**（检查点3 被 `battle_card.tscn` 取代），与 `[application] run/main_scene` 并存 | 任何触及 `[run]` 的路径（旧编辑器/工具/回退）都会指向死场景 |
| `scripts/`（6 脚本 + 6 `.uid`） | `battle.gd` / `ui.gd` / `board.gd` / `board_cell.gd`（`class_name BoardCell`）/ `bee_unit.gd`（`class_name BeeUnit`）/ `game_state.gd` | 死代码 + **两个全局类名**进入全局类表；工程结构歧义 |

**删除前引用核对（零引用才算）**：
```
$ grep -rn "res://scripts/" scenes/ project.godot
project.godot:21:GameState="*res://scripts/game_state.gd"      ← 唯一引用（本次一并移除）
$ grep -rn "GameState" scenes/ project.godot
project.godot:21:GameState="*res://scripts/game_state.gd"      ← 唯一引用
```
→ `scenes/**`（含全部 `.tscn` / `.gd`）**对 `scripts/` 与 `GameState` 零引用**，删除安全 ✅

## 二、处置（提交 `4e2132a` + 补提交 `d4a885e`）

```diff
 [autoload]
 _mcp_game_helper="*res://addons/godot_ai/runtime/game_helper.gd"
-GameState="*res://scripts/game_state.gd"
-
-[run]
-
-main_scene="res://scenes/battle.tscn"
```
```
D scripts/battle.gd      D scripts/battle.gd.uid        D scripts/bee_unit.gd   D scripts/bee_unit.gd.uid
D scripts/board.gd       D scripts/board.gd.uid         D scripts/board_cell.gd D scripts/board_cell.gd.uid
D scripts/game_state.gd  D scripts/game_state.gd.uid    D scripts/ui.gd         D scripts/ui.gd.uid
```

**过程教训（值得记账）**：首次提交 `git add -A project.godot scripts` 报
`fatal: pathspec 'scripts' did not match any files` → 因**已 `git rm` 的路径不能再作为 add 的 pathspec**，
导致**整个 add 未生效**、`project.godot` 被漏提。经 `git diff` 发现后补提交 `d4a885e` 修正。
→ **规矩**：提交后必须 `git status --porcelain` 复核；`git rm` 的删除项与未跟踪增项要分开处理。

## 三、验证 A（检查点5）：冷启动进入正确主场景，配置改动无副作用

- 重启游戏：`project_manage(op="stop")` → `project_run(autosave=false, mode="main")`，`run_token=5`
- **游戏日志**（`logs_read(source='game')`）：
  ```
  [godot_ai game_helper] registered mcp capture (debugger active=true, logger=true)
  [BattleFlow] 接线自检通过（26 项）
  ```
- **编辑器日志**：`total_count: 0`（无错误、无警告）✅
  → 关键证据：Godot 在启动时**重新读取 `project.godot`**；若 autoload 条目仍存在，此时必然报
  "无法加载 res://scripts/game_state.gd"。**日志干净即证明 autoload 已真正移除** ✅

## 四、验证 B（检查点3）：真实鼠标走通开局（功能无回归）

用 `game_manage(op="input_mouse")` 对主按钮位置 (1055, 366) 发**真实鼠标按下/抬起**：

| 项 | 点击前（对战准备） | 点击后 | 判定 |
|----|-------------------|--------|------|
| `InfoBar.text` | `第1回合 · 阶段:准备 · ` | `第1回合 · 阶段:部署 · ` | ✅ 阶段推进 |
| `BtnText.text` | `开始对局` | `进入行动` | ✅ 主按钮文本正常（`V-004-27` 历史事故点**无回归**） |
| `LogRich.text` | 对战准备提示 | `对战准备结束，开始第一回合` / `—— 第1回合 · [color=#499169]绿方[/color] ——` / `【回费】[color=#499169]绿方[/color] +2 → 费用 2（上限 10）` | ✅ 日志与**行内阵营染色**均正常 |
| 费用 | 绿方 0 | 绿方 **2** | ✅ a500 回费阶段生效 |

**读图复核**（`editor_screenshot(source='game')` + 读图）：顶部 `第1回合 · 阶段:部署 · `**`绿方`（绿）**` · 点牌选中…（再点自身=支援）`；
主按钮**`进入行动`**；日志块内 `绿方`/`红方` 分别上阵营色且无底色；双方手牌与费用徽章、技能详情区、地图与特殊地形格全部正常。

## 五、验证 C（检查点4）：删除后无任何残留引用，边界合规复核

```
$ grep -rn "AnimationPlayer\|Tween" scenes/ project.godot     → 仅 2 处**注释**（声明禁用），代码 0 处 ✅（T3）
$ grep -rn "verification/" scenes/ project.godot              → 0 处 ✅（T13 隔离铁律）
$ grep -rn "delta" scenes/                                    → 仅 _process(_delta) 签名占位与注释 ✅（T2）
$ grep -rn "res://scripts/" scenes/ project.godot             → (none) ✅（死目录已彻底摘除）
$ git status --porcelain                                      → 仅本文件（未跟踪）✅（工作树干净）
```

## 六、结论

**检查点 3 / 4 / 5 全部通过** ✅ —— **G-19、G-20 已修复并实机验证**：
- 死 autoload 与死场景声明移除后**冷启动一切正常**（自检 26 项、控制台零错误、开局可玩）
- 迭代001 残留 12 个文件（含 2 个全局类名）彻底摘除，**零残留引用**
- T2/T3/T13 三条红线复核通过；零新功能（仅删除与注释）
