# V-003.1-23 可维护性重构 第6块·上：拆出 `battle_setup.gd` · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代003.1 · 可维护性重构 第2步（拆分）第6块·上
> 封存日期：2026-08-27

## 改动

| 文件 | 改动 |
|------|------|
| **新增** `scenes/battle/battle_setup.gd`（100 行，RefCounted） | `start` · `prepare`（原 `_prepare`）· `draw_map` · `spawn` · `unit_at` |
| `battle_state.gd` | 5 个函数改为**同签名转发**（`_prepare` 保留原名转发至 `setup.prepare()`） |
| `battle_flow.gd` | 注入 `state.setup` + **启动自检增至 13 项** |

**前置扫描**：`hand[side] = []` / `deckl[side] = []` 属容器赋值风险 → 新脚本改用独立局部变量 `empty.duplicate()` ✅

## 验证（实机 eval，行为不变）

| 项 | 结果 |
|----|------|
| `state.setup` 注入 | `true` ✅ |
| `started` / `phase` | `true` / `4`（准备阶段）✅ |
| 构筑（a500 构筑 2） | 绿 手牌 4 + 备卡 4；红 手牌 4 + 备卡 4 ✅ |
| 蜂王部署 | 单位数 2（双方蜂王经 `setup.spawn` 生成）✅ |
| 地图 | 「蜜源」、特殊地形 2 格 ✅ |
| 先后手与费用 | 先手=red、`费用={green:2, red:0}`（**后手 +2 正确落在 green**）✅ |
| 换牌次数 | `{green:1, red:1}` ✅ |
| **`unit_at`（最高频函数）** | `unit_at((3,1)) = 1`（绿方蜂王）✅ |

## 硬规则执行情况

本轮严格按第5块立的规则：**一补丁一函数** + 补丁后 grep 自检 → **未再出现重复定义** ✅
（注：`grep -c "^func start"` 会同时匹配 `start_battle`，属**误报**，需用 `"^func start("` 精确匹配）

## 行数变化

| 文件 | 重构前 | 现在 |
|------|--------|------|
| `battle_state.gd` | 1175 | **791** |
| grid / pending / deck / deploy / action / setup | — | 92 / 104 / 157 / 112 / 181 / 100 |
| `battle_flow.gd` | 116 | 183（注入 + 13 项自检）|

## 下一块（第6块·下）

`battle_turn.gd`：`advance_phase` · `begin_turn` · `end_turn` · `_apply_terrain` · `surrender` ·
`refund_of` · `check_victory` · `queen_alive` · `queen_hp` · `round12_result` · `on_small_pressed` ·
`request_surrender` · `cancel_surrender`（回合流程与胜负，**牵连整局**，拆后必须跑完整对局流程回归）
