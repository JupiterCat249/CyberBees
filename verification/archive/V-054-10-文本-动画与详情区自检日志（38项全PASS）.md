# 迭代054 动画系统 + 详情区 自检日志（验证用 · 非生产）

> 状态：已封存（验证用，非生产文件）
> 对应迭代检查点：迭代054 · 动画接通 / 详情区接通 / 资源生成
> 封存日期：2026-09-17
> 执行场景：`verification/iter054_anim_detail_check.tscn`（专项）· `iter054_framework_check.tscn` · `iter054_e2e_check.tscn` · `gen_resources.tscn`

## 一、"接通前先两两对照"结果

### ① 详情区：脚本期望路径 vs 场景实际节点（逐条核对）
| 脚本期望 | 场景实际 | 结论 |
|---|---|---|
| `HUD/InfoPanel/CardName` | 存在（Label） | ✅ |
| `HUD/InfoPanel/SkillDesc` | 存在（Label） | ✅ |
| `HUD/InfoPanel/Attributes/Row1..4/Value` | 存在（4 个 Label） | ✅ |
| `HUD/InfoPanel/Attributes/Row1..4/Icon` | 存在（4 个 TextureRect） | ✅ |

→ **无需改场景**；但发现并修掉一处代码缺陷：`battle_scene.gd` 误调 `state.show_card_detail()`
（`GameState` 上没有该方法）→ 改为调自身。

### ② 动画：Action Unit 引擎 vs 素材场景的兼容性
- Clip 只操作 `CanvasItem.modulate` 与 `Control.position` → 与三个素材场景**完全兼容**
- 素材场景里的 `CrtAnim`（AnimationPlayer）属**人工处理的显像管特效**（D-2：不动），
  与 T3「禁引擎动画系统」不冲突——T3 约束的是**战斗动作动画**
- 飘字原用旧图集字形（card-system）→ 改**文字节点 + Label**（去掉 card-system 依赖）

## 二、动画 + 详情区专项自检：**38 项全 PASS / 失败 0**

### 详情区（10+8 项）
节点存在性 10 项（含 4 行 Icon）· 卡名绑定 · 描述含技能名与正文 ·
Row1=攻击 / Row2=生命 / Row3=移动 / Row4=射程（兵蜂）· 指令卡 Row1=伤害 / Row3=射程

### Action Unit（8 项）
注册 13 个 Pattern · 含受击抖动/单位退场/浮字上浮 ·
**帧数计时：10 帧动画恰好 10 帧**（T2）· 动画确实改表现（modulate 变化）·
block_ui 播放中锁 UI / 终止后解锁

### 抖动（4 项）
**位移总和归零（围绕原位对称，无基准漂移）0.000** · **高伤害延长时长（36 → 54 帧）** ·
同种子可复现 · 幅度随伤害上升（2.01 → 4.02）

### 退场时序（2 项）
立即开播（未立刻通知移除）· **动画播完后才通知移除（20 帧）**

### 资源（6 项）
`game_data/cards/熊蜂.tres` 存在 · 加载为 UnitData · **id 为合法 UUID** ·
数值 5/8/1/1 · 视觉立绘已引用 · 卡组资源存在

## 三、资源生成报告（`gen_resources.tscn`）
```
===== 回读校验 =====
  卡牌回读：20 / 20
  卡组回读：12 张，蜂王=金刚蜂王 → OK
  蜂王资源路径：res://game_data/cards/金刚蜂王.tres
  地图回读：丰饶（回费 +4 @ 第 [3, 9] 回合）
===== 资源生成报告 =====
  生成文件数：22
  错误数：0
  目录：res://game_data
```

## 四、⭐ 本轮捕获并修复的真实缺陷

| # | 现象 | 真因 | 修法 |
|---|---|---|---|
| 1 | **退场动画播完了但单位永不消失** | 旧引擎用 `_state` 做「pattern 的 `trigger_on_stop_call` 若引擎没有则转交」的兜底；我移植时**漏了这层** → 回调从不触发 | 补 `ActionUnit.owner_node` 兜底，驱动里设 `= self`。探针实证：修复前 0 次 / 修复后 20 帧触发 |
| 2 | 移动后节点位置停在中途值 | 位移类动画（MOVE_BY）最后一次 step 是"回原位"，但四舍五入留 1~2px 残差并**累积** | 引擎加 `instance_finished` 信号 → 驱动转发 `reposition_needed` → 接线层**收位**（吸附回格位） |
| 3 | 详情区从不刷新 | `state.show_card_detail()` —— `GameState` 无此方法 | 改为调自身 `show_card_detail()` |

## 五、截图
`V-054-9-图-详情区已接通（点手牌显示卡名·技能·四维）+可部署格高亮.png`
