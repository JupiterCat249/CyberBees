# V-003.1-9 被动源（机场）接线 —— 部署范围改由被动技能驱动 · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代003.1 · 检查点2（被动源部分）
> 封存日期：2026-08-27

## 删除的硬编码

`legal_place()` 的兵蜂分支原先直接调用 `adjacent_own_queen(cell)`（写死"蜂王相邻"），
与 a500「兵蜂部署在蜂王相邻格子」耦合在代码里，无法被技能改变。

## 新实现

```gdscript
"soldier":
    return within_deploy_radius(cell)      # 半径取自 deploy_radius[side]

func within_deploy_radius(cell) -> bool:   # 半径由【机场】被动写入
    ... <= int(deploy_radius.get(current, 1))

# 开局结算被动：start_battle() 中对双方蜂王 run_passive("机场", queen_id)
```

## 验证（实机 eval）

| 项 | 结果 |
|----|------|
| `deploy_radius`（start_battle 后） | `{green: 1, red: 1}` —— **由【机场】被动写入**（非硬编码） |
| 蜂王相邻格 | `within_deploy_radius` = `true` |
| 远端己方格 | `within_deploy_radius` = `false` |
| 指令源补充验证 · 治疗 | 己方 HP `1 -> 4`，日志「技能「治疗」生效（命中 1 处）」（走 `instant_heal`） |
| 指令源补充验证 · X费 | 敌 HP `20 -> 16`（= 目标部署费 2 × 2），日志「技能「X费·毁灭」生效（命中 1 处）」 |
| 指令源 · 补给 | **测试用卡「补给」不在卡池** → `D.card()` 回退为叶蜂 → 报「未找到技能定义：叶蜂」（**测试脚本缺陷**，非代码缺陷） |

## 待办

- 卡池（`POOL`）中补齐 回收 / 补给 / 轮换 三张指令卡，使技能表与卡池一致
- 检查点 3/4：`move` 位移类、`chain` 链式传播样例
