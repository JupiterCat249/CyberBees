# V-003.1-28 可维护性重构 节点7（二）：交互状态机**接线** · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代003.1 · 可维护性重构 节点7（接线）
> 封存日期：2026-08-27

## 改动

| 文件 | 改动 |
|------|------|
| `battle_state.gd` | `on_cell_clicked` 整段（原 65 行分支链）替换为 **转发 `interaction.dispatch_cell(cell)`**；新增 `on_cell_clicked_fallback(cell)` 作为优先级表第 11 条的出口（改选/取消） |
| `battle_flow.gd` | 注入 `state.interaction` + **启动自检增至 16 项** |

**本次成功的关键**：按上一轮定下的方法 —— 先用 `sed -n '/^func on_cell_clicked(/,/^# ===/p'`
取出**完整原文（含中文注释）**，再整段替换为 `old_text` → **一次命中** ✅（对比此前凭记忆写 old_text 连续失败 3 次）

## 验证（实机 eval，经新裁决入口）

| 步骤 | 结果 |
|------|------|
| `state.interaction` 注入 | `true` ✅ |
| 优先级表条数 | 11 ✅ |
| ① 点单位格 | 选中=3、状态可推导、**长按允许=false**（非空闲态禁止，正确）✅ |
| ② 点友方目标 | `pending=[支援]`、状态=待确认 ✅（**二次确认到达**）|
| ③ 再次点同格 | `pending=[]`；日志「泥蜂 获得效果「护甲」」「技能「护卫」生效（命中 1 处）」「熊蜂 支援 泥蜂」✅（**执行成功**）|

## 节点7 达成情况

| 要求 | 状态 |
|------|------|
| 新建 `battle_interaction.gd`（显式状态机 + 点击优先级表） | ✅（V-003.1-27）|
| 接线到 `battle_state` 点击入口，行为不变 | ✅ 本次（`on_cell_clicked` → dispatch）|
| 收敛 `pending_kind/armed_card/selected_unit/mode` 隐式耦合 | ✅ 裁决全部集中到 `dispatch_cell` + 优先级表；`derive()` 提供**唯一的状态推导** |
| **真实鼠标/键盘回归**（支援二次确认/部署/指令/移动/攻击/换牌/投降） | ⏳ **未完成**（本轮为 eval 级回归）|
| 复用 `verification/suites/` 套件 | ⏳ **未完成** |
| T13 封存 + 提交推送 | ✅ |

## 状态与行数

| 文件 | 起始 | 现在 |
|------|------|------|
| `battle_state.gd` | 1175 | **~660** |
| `battle_interaction.gd` | — | ~110 |
| `battle_flow.gd` | 116 | 216（注入 + 16 项自检）|

## 下一步（完成节点7 验收）

1. **真实鼠标回归**（MCP `input_mouse` + 真实坐标 ×0.6）：支援二次确认 / 部署 / 指令 / 移动 / 攻击 / 换牌 / 投降
2. 复用并扩展 `verification/suites/`：新增「交互状态机」套件（各状态下的点击应落到优先级表的哪一条）
3. 全绿后按 T13 封存 → 节点7 完成 → 进入收尾（去转发壳 + 全仓扫描 + 更新系统维护文档）
