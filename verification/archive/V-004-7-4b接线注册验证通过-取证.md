# V-004-7 迭代004 · 第4b步：接线**注册验证通过** · 取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代004 · 第4b步（表现时机接入）—— 注册层验证
> 封存日期：2026-08-27

## 本轮方法（按 V-004-6 沉淀的方案执行，**不再试错**）

1. `stop()` 清掉被挂住的游戏实例
2. **重新接线**（协调器注入 `node_provider` + `an.bind(state)`），编译无诊断 ✅
3. `project_run` → 确认 `game_status.status == "live"` ✅
4. **单条零副作用只读 eval**（不推进状态、不生成单位、不攻击、无循环）✅

## 验证结果（实机 eval，只读）

| 项 | 结果 |
|----|------|
| `state.anim` 注入 | `true` ✅ |
| 已注册 Pattern 数 | `7`（受击抖动 / 受伤闪红 / 地图抖动 / 卡牌登场 / 卡牌退场 / 回合色-我方 / 回合色-敌方）✅ |
| **`combat.attack_resolved` 已连接** | `true` ✅ |
| **`state.turn_started` 已连接** | `true` ✅ |
| **`state.battle_ended` 已连接** | `true` ✅ |
| **`state.state_changed` 未连接**（已废弃的重绘驱动） | `false` ✅（**V-004-4/5 的改进生效**）|
| `node_provider` 已注入 | `true` ✅ |
| 游戏运行 | 正常（`phase=0 current=green 单位=0`）✅ |

**结论**：表现时机的**接线注册成功且未污染**（既接上了三条低频语义信号，又没有保留高频重绘驱动），
且游戏在该接线存在下**正常运行**（未因接线崩溃）✅

## 仍未完成（迭代004 剩余）

| 项 | 说明 |
|----|------|
| **受击动画的视觉取证** | 需**真实鼠标**走 开始对局 → 部署 → 行动 → 攻击，并用 `pilot_screenshot`/`editor_screenshot` + `read_image` 复核（T13 要求 `reviewed: true` 的视觉证据）|
| 数值文本落地 | 回费 `#FFA300` / 受伤 `#FF2000` / 回血 `#00DD00` 飘字 + **字号随数值** |
| AOE 地图抖动接入 | `shake_map()` 已就绪，需在技能命中多目标（溅射/链式）处调用 |
| UI 回合色应用 | `turn_color()` / 回合色 Pattern 已就绪，需挂到 HUD |
| 瞬间动作无前摇 | `卡牌登场` 已按"第 1 帧即到位"实现，待视觉确认 |
| 建档 + 验收 + tag | 系统维护（基础描述/API/**扩展-如何新增动画**）· 迭代004 记录与结论 · tag `iter/004` |
