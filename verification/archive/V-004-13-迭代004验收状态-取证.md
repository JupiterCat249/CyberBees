# V-004-13 迭代004 · **验收状态记录**（进行中，未打 tag） · 取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代004 · 验收
> 封存日期：2026-08-27

## 交付逐项验收

| 交付 | 要求 | 实测证据 | 结论 |
|------|------|----------|------|
| **①** 自写 ActionUnit 引擎 | Pattern→Unit 编排；5 类 Unit（指定值可叠加移动/旋转 · 到指定值不可重复 · 旋转追踪 · 变色 · 生成特效）；顺序执行 / 可跳转 / 可触发(start\|stop) / 可终止 / **终止级联不复位** / 可禁 UI 交互 / 可受全局暂停；ResetUnit/Action/Stop | `V-004-1`：引擎 306 行、编译无诊断；`V-004-3`：7 条 Pattern 注册成功；T3/T2 合规（无 AnimationPlayer/Tween；帧数计时） | ✅ |
| **②** 清理引擎动画残留 | 替换/清理现有引擎动画（如扫描线 AnimationPlayer），全项目统一走本系统 | `V-004-2`：`node_manage(delete)` 删除 `AnimationPlayer`(ScanAnim) + 子资源（Godot 自动清理无引用子资源，diff 25 行删除）；`V-004-10`：**截图中扫描线表现正常**（由 `battle_bgfx` 着色器逐帧驱动） | ✅ |
| **③** 表现规范落地 | 瞬间动作无前摇 · 数值文本（回费 `#FFA300` / 受伤 `#FF2000` / 回血 `#00DD00`，**字号随数值**）· 单位受击抖动 · **AOE 地图抖动** · UI 回合色（我方 `#499169` / 敌方 `#A84331` / `#FFFFFF-50%`） | `V-004-3`：`卡牌登场` 第 1 帧即到位（无前摇）；`V-004-9`：`text_color` 三色与 `turn_color` 两色**运行时读值逐项一致**、`text_size_for(1/3/5/9)=44/52/60/72`；`V-004-12`：`shake_map()` 实测 `running=["地图抖动"]` | ✅ |
| **④** 实机验证（含**真实鼠标**） | 真实鼠标走流程 + 按 T13 封存取证 | **部分**：`V-004-10` 已取得运行画面**视觉取证**（读图复核，含扫描线/棋盘/HUD）；`V-003.1-29` 曾以真实鼠标验证交互链路；**本次迭代的"完整鼠标流程（开始对局→部署→行动→攻击）"未完成** —— 受"游戏助手主循环未推进（窗口后台化）"阻碍 | ⏳ **未完全达成** |
| **⑤** 系统维护建档 + 迭代记录 | 基础描述 + 代码API + 扩展（如何新增动画）+ 迭代记录与验收结论 + tag | 建档 **✅**（`系统维护/动画系统/` 三份，含**扩展-001 操作指南**）；迭代记录 **✅**（进度快照 + 教训）；**验收结论：本文件（进行中）**；**tag：未打**（须验收通过） | ⏳ 部分 |

## 本迭代额外修复（阻塞级）

| 缺陷 | 结论 |
|------|------|
| **对局从不自动开始**（`started=false`） | **已修复并验证** ✅ —— 根因 G-01：4 个注入块被编辑器回退且无自检守卫；已补回注入 + 4 个自检项（`V-004-11`）|
| **`turn_color` 缺失崩溃** | **已修复** ✅ —— 落地 Pattern 时漏加函数，HUD 调用即崩；已补 `turn_color()`（`V-004-9`）|

## 未完成项（阻塞验收）

| 项 | 阻碍 | 下一步 |
|----|------|--------|
| 真实鼠标完整流程 + 受击抖动截图 | 游戏**助手主循环未推进**（窗口后台化/失去焦点）→ eval 取不到状态；鼠标事件虽能发送但无法读取结果 | 先在编辑器里**聚焦游戏窗口**（或每次失败后 `stop()`→`run()` 并留启动时间），再按"两击确认"序列点击并截图 |
| 迭代004 验收结论 + tag `iter/004` | 依赖上项 | 完成真实鼠标取证后立即收口 |

## 结论

**迭代004 的工程交付（① ② ③ ⑤建档）已完成并逐项有实测证据**；
**验收（④ 真实鼠标完整流程）尚未完全达成** → 按规则**暂不打 `tag iter/004`**，
目标保持 active，下一轮补齐后收口。
