# V-003.1-1 支援技能走结构化管线 · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代003.1 · 检查点1（支援源接线 + 删除硬编码分支）
> 封存日期：2026-08-27

## 验证方式

`mcp__godot-ai__editor_manage(op="game_eval")` 实机取值；测试单位运行时生成，不写入生产资源。

## 现象与根因

- 现象：支援技能有 UI 反应（蓝色候选高亮），但**始终走不到确认与执行**。
- 取证：`confirm()` 返回 true（pending 已清空）→ `support_at()` 被调用，但**零日志零效果**。
- 根因：**`battle_flow.gd` 的 `state.skills = sk` 注入整段丢失** —— 编辑器脚本缓冲把旧版本写回磁盘
  （G-01 已知问题重现），`use_skill()` 因 `skills == null` **静默返回**（该分支原先无日志，放大了隐蔽性）。
  同批丢失的还有详情浮窗/取消选中/长按/投降确认等 5 处接线。
- 恢复：`git checkout HEAD -- scenes/battle_flow.gd`（行数 92，接线齐全）。

## 验证结果（修复后）

| 步骤 | 结果 |
|------|------|
| 技能系统接入 | `true` |
| 点自身进入支援选择 | `mode=3`，`support_range=[(0,2),(0,1)]` |
| 两次点友方（二次确认） | 泥蜂 `effects=["atk_up"]`、攻击 3→4；叶蜂 `acted=true` |
| 日志 | `技能「鼓舞」生效（命中 1 处）` / `叶蜂 支援 泥蜂` |

## 结论

支援技能已改走 `[源]→[条件]→[倍率]→[对象]→[效果]` 四段管线并可用；旧硬编码支援分支已删除。

## 遗留

- `use_skill()` 的 `skills == null` 分支**无日志**（静默失败）→ 已列入改进项（应补明确报错）。
- G-01 缓解措施：每次编辑器侧操作后执行 `git diff --stat` 比对，发现回退立即从 HEAD 恢复。
