# V-003.1-17 检查点7（描述自动生成）· 检查点8（纯数据技能） · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代003.1 · **检查点7 / 检查点8**
> 封存日期：2026-08-27

## 检查点7：技能描述由结构化数据自动生成 ✅

新增 `BattleSkills.describe(def)`：源 / 对象（模式·阵营·类型·距离·链式）/ 条件条数 / 倍率 / 效果
全部从定义里读出，取代手写 `sdesc`（避免描述与实现脱节）。
配套 `_effect_cn()` 按效果类型生成中文短语，`_count_leaves()` 统计条件叶子数。

**实测输出**（技能表 17 条）：

| 技能 | 自动生成描述 |
|------|--------------|
| 精确打击 | 使用指令卡，对地格（溅射 1），仅敌方，限兵蜂，效果：5 点伤害。 |
| 巡航导弹 | 使用指令卡，对地格（溅射 1），效果：4 点伤害。 |
| 蜂群突袭 | 使用指令卡，对单体，仅敌方，距离 3，条件 2 条，倍率 ×1.5，效果：4 点伤害。 |
| 工蜂修筑 | 支援技能，对单体，仅己方，距离 1，效果：赋予「坚固」(hp_add+2)。 |
| 蜂粮储备 | 使用指令卡，作用于自身，条件 1 条，效果：费用 +2。 |
| 鼓舞 | 支援技能，对单体，仅己方，距离 2，条件 1 条，效果：赋予「攻击提升」(atk_add+1)。 |

## 检查点8：纯数据新增技能（不改任何逻辑代码）✅（部分）

新增 3 条**仅数据**技能（未改任何执行逻辑）：

| 技能 | 覆盖能力 | 验证 |
|------|----------|------|
| 蜂群突袭 | 多条件(all) + **倍率 ×1.5** + 阵营过滤 | ⏳ 待补卡后复验 |
| 工蜂修筑 | `hp_add`（最大生命提升） | ✅ `final_hp_max 4 -> 6`，`effects=["tough"]` |
| 蜂粮储备 | 费用条件 + 资源类效果（自身目标） | ⏳ 待补卡后复验 |

### 未能实测的两条：**卡池缺卡**（已知不一致，非技能系统缺陷）

`蜂群突袭` / `蜂粮储备` 在 `SKILLS` 中有定义，但 `POOL`（卡池）中**没有对应卡牌** →
`D.card("蜂群突袭")` **回退为别的卡** → `cmd_at` 用回退卡的卡名触发技能 → 自然不生效 ✗。

> 这正是《扩展-002》里已记录的已知不一致：**技能表与卡池必须同名对应**。
> **卡池补齐清单**：回收 / 补给 / 轮换（已补）· **蜂群突袭 / 蜂粮储备（待补）**

## 附带修复

补丁过程中一度**误删**检查点6 的 `精确打击` 样例（old/new 文本未完整保留），已立即补回并复验技能表完整（17 条）。

## 教训

**改数据表时，`old_text` 必须完整覆盖被替换块**，否则会静默丢条目 ✗；
新增/删除技能条目后应核对 `SKILLS.size()`（本轮 17 条）与卡池一致性。
