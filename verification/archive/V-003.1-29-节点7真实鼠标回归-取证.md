# V-003.1-29 节点7（三）：**真实鼠标**回归 + 交互状态机套件 · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代003.1 · 可维护性重构 节点7（验收）
> 封存日期：2026-08-27

## 真实鼠标回归（MCP `input_mouse`，真实屏幕坐标 ×0.6）

场景：行动阶段，绿方「叶蜂」@(0,0)、「泥蜂」@(0,1)（叶蜂支援技能=鼓舞）

| 步骤 | 真实操作 | 结果 |
|------|----------|------|
| 1 | 点叶蜂格 **(351,99)** | `选中=3`（经 `BattleInput` → `on_cell_clicked` → **状态机** → 选中）✅ |
| 2 | 点泥蜂格 **(501,99)** | `pending=[支援] cell=(0,1)`、**状态=待确认**、日志「待确认：支援 @(0,1)…」✅ |
| 3 | 再点同格 **(501,99)** | `pending=[]`、**状态=空闲**、施放者 `acted=true`、目标 `effects=["atk_up"]` `atk=4`、日志「泥蜂 获得效果「攻击提升」/ 技能「鼓舞」生效（命中 1 处）/ 叶蜂 支援 泥蜂」✅ |

**结论**：真实鼠标路径下，"选中 → 待确认 → 执行 → 回到空闲"整链经**显式状态机**裁决，行为正确 ✅

## 新增可复用套件

`verification/suites/V-suite-交互状态机.gd.txt` —— 三条**结构性断言**（针对本迭代反复出问题的点）：
1. **待确认优先于模式**：`pending` 存在时点同格必执行（不依赖 `mode`/选中是否仍在）
2. **支援待确认点候选外 → 退出该状态**
3. **非空闲态禁止长按**（`can_long_press` 仅 IDLE 为 true）

## 仍未完成（故目标保持 active）

| 项 | 状态 |
|----|------|
| 真实鼠标：**支援二次确认** | ✅ 本轮完成 |
| 真实鼠标：部署 / 指令 / 移动 / 攻击 / 换牌 / 投降 | ⏳ 待做（此前为 eval 级验证）|
| 复用/扩展 `verification/suites/` | ✅ 本轮新增交互状态机套件（其余套件待逐条回跑）|
