# V-003.1-24 可维护性重构 第6块·下（之一）：拆出 `battle_victory.gd` · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代003.1 · 可维护性重构 第2步（拆分）第6块·下（胜负子集）
> 封存日期：2026-08-27

## 说明：为何把"第6块·下"再切成子集

原计划的 `battle_turn.gd` 含 13 个函数（回合流程 + 地形 + 投降 + 胜负），**牵连整局**且体量大。
按"单次改动可控"原则，先拆**最内聚、无副作用**的胜负子集（4 个纯查询/判定函数），
`advance_phase` / `begin_turn` / `end_turn` / `_apply_terrain` / `refund_of` / 投降三件套留作下一子集。

## 改动

| 文件 | 改动 |
|------|------|
| **新增** `scenes/battle/battle_victory.gd`（63 行，RefCounted） | `check_victory` · `queen_alive` · `queen_hp` · `round12_result` |
| `battle_state.gd` | 4 个函数改为**同签名转发**（整组一次替换，`old_text` 完整覆盖 4 个函数） |
| `battle_flow.gd` | 注入 `state.victory` + **启动自检增至 14 项** |

## 验证（实机 eval，行为不变）

| 项 | 结果 |
|----|------|
| `state.victory` 注入 | `true` ✅ |
| `queen_alive` | 绿 `true` / 红 `true` ✅ |
| `queen_hp` | 绿 `8` / 红 `8` ✅ |
| **胜负判定** | 击杀绿方蜂王 → `winner=red`、日志「游戏结束：红方 获胜」✅ |
| 第12回合比较 | ⏳ **未验证**（测试先移除绿蜂王导致取不到其 id，属测试构造问题）|

## 硬规则执行情况

- 采用**"整组连续函数一次替换"**：`old_text` 完整覆盖 4 个函数 → 无重复定义 ✅
  （比"一补丁一函数"更省调用，安全性等价 —— 前提是 old_text 覆盖被替换的**全部**函数）
- 精确自检：`check_victory(` / `queen_alive(` / `queen_hp(` / `round12_result(` 均为 **1** ✅

## 行数变化

| 文件 | 重构前 | 现在 |
|------|--------|------|
| `battle_state.gd` | 1175 | **773** |
| grid / pending / deck / deploy / action / setup / victory | — | 92 / 104 / 157 / 112 / 181 / 100 / 63 |
| `battle_flow.gd` | 116 | 194（注入 + 14 项自检）|

## 下一子集

`battle_turn.gd`：`advance_phase` · `begin_turn` · `end_turn` · `_apply_terrain` · `refund_of` ·
丧降三件套（`surrender` / `request_surrender` / `cancel_surrender`）· `on_small_pressed`
—— 拆后必须跑**完整对局流程回归**（准备→部署→行动→回合切换→胜负）。
