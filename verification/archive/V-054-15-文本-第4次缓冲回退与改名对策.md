# 迭代054 第 4 次缓冲回退 → 控制器改名对策（验证用 · 非生产）

> 状态：已封存（验证用，非生产文件）
> 对应迭代检查点：迭代054 · 回退治理（二）
> 封存日期：2026-09-17

## 一、人反馈
> 「修改疑似回退了：我注意到 battle_scene 场景里手牌和单位卡面显示是默认值，且交互逻辑等似乎消失了。」

## 二、核查结果（真相）
| 对象 | 状态 |
|---|---|
| `scenes/ui/battle_scene.gd`（控制器） | ❌ **被退回 256 行旧版**（时间戳 23:08），本轮全部逻辑丢失；**完整版安全存在 git HEAD（730 行）** |
| `scenes/ui/battle_scene.tscn`（场景） | ✅ 完好（16 格 + 双方单位 + 双方各 4 张手牌 + Portrait） |
| `scenes/ui/card_unit.gd` | ✅ 完好（含 `@tool` 与立绘绑定） |

**根因确认**：这是**第 4 次**发生同样的编辑器脚本缓冲回写
（此前 3 次：22:23 / 22:40 / 22:44）。已开启的
`text_editor/behavior/files/auto_reload_scripts_on_external_change` **不足以完全阻止**。

## 三、对策一：控制器**换文件名**（从根上断开覆盖链）
- `scenes/ui/battle_scene.gd` → **`scenes/ui/battle_arena.gd`**（`git mv`）
- 编辑器旧缓冲绑定的是 `battle_scene.gd` 这个**路径**，改名后**再也无法命中**
- 同步更新：`battle_scene.tscn` 的 `ext_resource` script 引用 + 全部 verification 脚本引用
- 删除旧 `battle_scene.gd.uid` 边车
- 场景名与节点内容**不变**（人仍然打开 `res://scenes/ui/battle_scene.tscn`）

## 四、对策二：卡面数值**直接烤入场景**
问题：卡组件的数值原本只在 `bind()` 里写入 → 编辑器里若脚本未运行就显示默认 0。
现在把真实数值作为 **static override** 写进 `.tscn`：

| 对象 | 烤入内容 |
|---|---|
| 单位卡（双方蜂王） | 费用 `8` · 攻击 `2` · 生命 `15` · 移动 `1` · 射程 `1` |
| 手牌卡（左右各 4 张） | 徽章费用 `2` + 立绘引用 |

**效果**：编辑器里打开场景即见真实数值（截图 `V-054-16`），不再依赖脚本是否运行。

## 五、验证
| 项目 | 结果 |
|---|---|
| 命令行全量语法检查（`--check-only`） | ✅ 所有脚本通过 |
| UI 端到端自检 | **34 / 0**（红方控制 6 项 + 立绘同步 2 项全在） |
| 规则层自检 | **76 / 0** |
| 编辑器实测 | 手牌徽章=2 · 单位卡 费用8/攻击2/生命15/移动1/射程1 |

## 六、注意事项（给后续维护）
1. **编辑器内那两条 `card_visual.gd` / `board.gd` 报错是编辑器内存缓冲的陈旧版本**，
   磁盘上的文件经命令行 `--check-only` 验证**全部通过** → 关闭并重新打开这两个脚本即可消除。
2. 若日后再出现"改了却没生效"，先执行：
   `git status` → `git log --oneline -3` → 确认磁盘文件行数是否与 HEAD 一致（这是最快的回退判定）。
