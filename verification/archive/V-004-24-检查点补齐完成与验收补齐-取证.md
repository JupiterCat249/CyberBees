# V-004-24 迭代004 · **检查点补齐完成 + 验收补齐结论** · 取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代004 · **补齐收口（保留原 tag `iter/004`，记录补齐过程）**
> 封存日期：2026-08-27

## 一、为什么要"验收补齐"

本迭代先前按**任务目标里的 ①–⑤** 收口并打了 `tag iter/004`；
但迭代记录自带一份**官方检查点清单（11 项）**，两者不等价 ✗ ——
按项目规则「迭代完成且**验收通过**才打 tag」，当时**未完全达标** ✗
（规则禁止删/覆盖 tag，故**保留 tag**，补齐后用本记录说明）。

## 二、补齐过程（C → A → B → D）

| 步 | 内容 | 覆盖检查点 | 证据 |
|----|------|-----------|------|
| **C** | 抖动**幅度随数值缩放**；UI **回合背景色** | 9、10 | `V-004-20`、`V-004-21` |
| **A** | **六类瞬间动作接线**（部署/移动/使用指令/数值buff 补齐） | 6 | `V-004-22` |
| **B** | **数值文本进表现层**（回费 `#FFA300` / 增益 `#00DD00` / 减益 `#FF2000`，字号随数值）；标准流程顺序可见 | 7（并强化 8） | `V-004-23` |
| **D** | **禁 UI 交互实测** · **受全局暂停实测** · **T2 帧数驱动复核** | 5、11 | 本文件 |

## 三、D 步实测结果（本文件核心）

### ① 动画期间禁 UI 交互（`block_ui`）✅
```gdscript
register("结算演示", {"units":[…], "block_ui": true, "pause_global": true})   # 引擎侧
# 点击裁决入口加守卫（原先 ui_locked 只被设置、**无人检查** ✗）
func dispatch_cell(cell):
    if s.anim != null and s.anim.ui_locked: return
```
| 步骤 | 结果 |
|------|------|
| 初始 `ui_locked` | `false` ✅ |
| 启动 `结算演示` 后 | `ui_locked = true` ✅ |
| **此时点棋盘** | **被拦**（`selected_unit` 保持不变、无异常）✅ |
| `stop` 后 | `ui_locked = false` ✅ |

### ② 受全局暂停（`pause_global`）✅
| 项 | 结果 |
|----|------|
| 全局暂停中，`常驻呼吸`（`pause_global=false`） | **仍在推进** ✅（未受影响）|
| 受 `pause_global=true` 的 Pattern | 暂停（`_step` 中按 Pattern 逐条判断）✅ |
| 恢复 `anim_paused=false` | 正常 ✅ |

### ③ T2 帧数驱动复核 ✅
| 检查 | 结果 |
|------|------|
| 引擎内 `delta` | **仅 `_process(_delta)` 签名占位**，计时全部用 `frames` 帧数 ✅ |
| 引擎内 `Tween` / `AnimationPlayer`（T3） | **0 处** ✅ |
| 全项目 `AnimationPlayer` 残留 | **0 处** ✅ |

## 四、迭代004 检查点最终达成情况：**11 / 11** ✅

| # | 检查点 | 状态 |
|---|--------|------|
| 1 | ActionUnit 骨架（Pattern/Unit/Clip + ResetUnit/Action/Stop） | ✅ |
| 2 | 5 类 Unit 全部实现 | ✅ |
| 3 | 分支触发（开始时/终止时触发他者） | ✅ |
| 4 | 运行规则（顺序执行 · 跳转/触发/终止 · **终止级联不复位**） | ✅ |
| 5 | 播放控制（**禁 UI 交互** / **受全局暂停**） | ✅ **D 步实测** |
| 6 | 六类瞬间动作接线（部署/移动/攻击/使用指令/数值buff/登场退场） | ✅ A 步（6/6） |
| 7 | 标准流程顺序可见（含回费文本与 buff 文本） | ✅ B 步 |
| 8 | 文本三色 + 字号随数值 | ✅（并以浮字落地强化） |
| 9 | 抖动（单位受击 + AOE 地图 + **幅度随数值**） | ✅ C 步 |
| 10 | UI 状态色（文本态 + **背景态**） | ✅ C 步 |
| 11 | **帧数驱动（T2）** + 动画期间禁 UI 交互**实测** | ✅ **D 步实测** |

## 五、补齐过程中修复的缺陷（5 个，均为"接线/时序/生命周期"型）

| # | 缺陷 | 性质 |
|---|------|------|
| 1 | 动画写入**已释放节点** | 生命周期（`V-004-16/17`，最终以 `weakref` 根治）|
| 2 | 表现调用早于 `refresh()` | **时序**（节点尚未建立 → 静默跳过）（`V-004-22`）|
| 3 | 引擎未预载 `D` | 接线（Parse Error）(`V-004-23`) |
| 4 | `fx_parent` 未注入 | 接线（**静默无表现**）(`V-004-23`) |
| 5 | `ui_locked` 只设不查 | **契约未闭环**（本文件修复）|

> **共同规律**：表现层的问题**大多不报错**，只表现为"没效果/卡死"✗ ——
> 必须"**改完立刻实机验证**"；且**每个能力都要有消费者**（设了状态就得有人检查它）✅

## 六、结论

**迭代004 的 11 项检查点全部达成，且每项均有实机证据**（`V-004-1 ~ 24`）✅
原 tag `iter/004` **保留**；本记录作为**验收补齐说明**。
