# V-003.1-31 可维护性重构 **收尾完成** · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代003.1 · 可维护性重构 收尾阶段
> 封存日期：2026-08-27

## ① 全仓扫描结果

| 检查项 | 结果 |
|--------|------|
| T11 card-system 只读 | **0 处修改** ✅ |
| T13 生产代码禁引用 verification | **0 处** `load/preload("res://verification/…")` ✅ |
| verification 禁放 `.gd` | **0 个** `.gd` 文件 ✅ |
| `verification/.gdignore` | **在位** ✅（引擎忽略该目录，天然不导出）|
| 启动接线自检 | **16 项**，全部注入 `true` ✅ |

### 脚本体积

| 文件 | 行数 | 说明 |
|------|------|------|
| `battle_state.gd` | **632** | 起始 1175（↓543，降 46%）；仅 Model 职责 |
| `battle_skills.gd` | 461 | 技能系统（可独立测试）|
| `battle_defs.gd` | 444 | **纯数据**（常量表，行数大属正常）|
| `battle_flow.gd` | 215 | 协调器 + 16 项自检 |
| 其余 8 个 | ≤204 | 均已 ≤300 目标 ✅ |

### 死代码 / 死数据核查

| 项 | 结论 |
|----|------|
| `adjacent_own_queen` | **零调用** → **已删除** ✅（部署范围自迭代003.1 起由被动 `deploy_radius` 驱动）|
| 卡面 `dmg` / `heal` | ⚠️ **更正此前判定**：**并非死数据** —— 仍被 3 处读取（`battle_defs` 卡面渲染 / `battle_deploy` 治疗目标校验 / `battle_detail_view` 文案）→ **保留不动** ✅ |

> 教训：判定"死数据"必须用**足够宽的检索模式**（`["dmg"]` 而非 `card["dmg"]`），
> 否则会得出错误结论并误删生产数据。

## ② 系统维护文档

| 文档 | 内容 |
|------|------|
| **新增** `系统维护/战斗节点体系/扩展-004-节点拆分与交互状态机.md` | 拆分后结构图 · 交互状态机说明 · **11 条点击优先级表** · **【操作指南】如何新增交互规则/新状态**（4 步 + 4 条禁止 + 3 条约束）· 开发约定 |
| 更新 `系统维护/战斗节点体系/基础描述.md` | 新增"结构现状（可维护性重构后）"段 |

## ③ 回归验证（实机 eval）

| 项 | 结果 |
|----|------|
| 10 个注入（grid/pending/deck/deploy/action/setup/victory/turn/interaction/skills） | 全部 `true` ✅ |
| 死代码确认 | `adjacent_own_queen` **已不存在** ✅ |
| 断言③ 非空闲态禁长按 | 空闲 `true` → 选中后 `false`（状态=已选单位）✅ |
| 断言① 待确认优先 | 点目标→待确认；再点→`pending=[]`、状态=空闲、施放者 `acted=true`、目标 `effects=["atk_up"]` ✅ |
| 断言② 支援待确认点候选外→退出 | `pending=[支援]` → 点候选外 → `pending=[]`、状态=空闲 ✅ |

## 收尾阶段达成情况

| 项 | 状态 |
|----|------|
| 全仓扫描 + 修复（含死代码清理） | ✅ |
| 边界约束（T11/T13）复核 | ✅ 全部干净 |
| 系统维护文档更新（含新增交互规则操作指南） | ✅ |
| 套件回归 | ✅ 交互状态机套件 3 条断言全过 + 10 项注入核验 |
| T13 封存 + 提交推送 | ✅（游戏仓 + 文档仓）|

## 关于"去转发壳"的决策（未执行，说明理由）

`battle_state` 中为各模块保留的**同签名转发**（共约 40 个函数）**本轮未去壳**，理由：
1. 转发壳**无行为风险**、且让调用点（View/输入层）保持稳定；
2. 去壳需改动大量调用点（含 4 个 View），**收益（再降约 100 行）远小于风险**；
3. 当前单脚本已达 **≤632 行**、职责清晰、**16 项自检**兜底 —— 可维护性目标已达成。
→ 若后续确需进一步降行数，可**按模块分批去壳**，每批走完整回归（沿用本次的 `old_text` 规则）。
