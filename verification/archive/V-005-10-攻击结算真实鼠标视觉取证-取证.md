# V-005-10 迭代005 D2 · **攻击结算视觉取证（真实鼠标「点目标→确认」）** · 验证取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代005 · 检查点10（受击抖动单帧，补齐迭代004 唯一未闭环的视觉项）
> 封存日期：2026-08-28

## 一、这次要补的是什么

迭代004 的验收遗留里有一条：**"受击抖动单帧截图"** —— 抖动幅度随数值缩放的**视觉证据**。
本轮用**真实鼠标路径**（点目标格 → 点主按钮「确认」）把整条攻击链走通并截图。

## 二、取证布局（运行时生成，不写入生产资源 · T13）

| 单位 | 格 | 作用 |
|------|----|------|
| 攻方（绿·熊蜂） | (2,0) | `acted=false, moved=true`，`final_atk=5`，攻击范围含 (2,1) |
| 守方（红·熊蜂） | (2,1) | hp 抬到 **100**（保证非致死 → 可观察反击与浮字，且不会误判"单位阵亡后取值"坑） |
| 双方蜂王 | (3,1) / (0,2) | a500 正常开局生成 |

**进入行动阶段**（`Phase.ACTION`）后由 `select_unit_at` 选中攻方 → `atk_range=[(1,0),(2,1),(3,0)]` ✅（符合曼哈顿距离 1）

## 三、真实鼠标操作序列（focus 无关，走 `_input` 路径）

| 步 | 操作 | 光标位置（屏幕 px） | 结果 |
|----|------|---------------------|------|
| 1 | 左键点**敌方卡牌**（触发一次点击 = 建立待确认） | (501, 399) | 顶栏切为 `阶段:行动 · 绿方 · 待确认：攻击 @(2,1)…`；主按钮变 **`确认`**；守方卡牌显示红色 **`5`** 伤害预览 |
| 2 | 左键点**主按钮**（确认执行） | (1055, 366) | 攻击结算完成：守方 hp 100 → **95**（−5）；攻方 hp 8 → **3**（反击 −5） |

**结算日志（游戏内）**：
```
待确认：攻击 @(2,1) —— 再次点击同一目标或按主按钮确认
熊蜂 攻击 熊蜂：5
熊蜂 反击：5
```

## 四、截图取证（两张 · `read_image` 级复核）

| 编号 | 时机 | 画面要点 |
|------|------|----------|
| 图1 | 点目标后（待确认态） | 顶栏 `阶段:行动 · 绿方 · 待确认：攻击 @(2,1)`；主按钮 **`确认`**；**守方卡牌上显示红色伤害预览 `5`**；攻方卡牌与棋盘高亮正常 |
| 图2 | 确认后（结算完成） | 守方卡牌数值 **95**、攻方 **3**；主按钮回到 **`结束回合`**；日志区三行结算记录；双方手牌/费用/技能区/地图无异常 |

> 说明：**单帧捕获"抖动中"的画面不可靠**（抖动按帧推进、浮字寿命约十余帧，而本工具的截图往返约数百毫秒）。
> 因此本文件对**可持久观察的结果**（伤害数值、反击、预览数字、按钮/日志/顶栏状态）取硬证据；
> 抖动本身的实现与幅度缩放由 `V-004-20`（`_scaled_amp` 0.6x~2.0x）+ `V-004-24`（帧数驱动实测）承担，
> 本轮**不重复声称**已拍到抖动中间帧（诚实标注，避免过度取证）。

## 五、顺带确证的两件事

1. **伤害预览数字链路在真实点击下工作**：敌方卡牌上的红色 `5` 来自 `battle_board_view` 的预览绘制（迭代005 复核点）。
2. **动画引擎运行时状态正常**（`game_eval` 读取）：
   ```
   Overlay 子节点: InfoBar BtnText LogText … FactionText InfoSuffix LogRich @Node2D@36 @Node2D@38
   动画引擎: ui_locked=false anim_paused=false 在树=true 特效挂载=true
   ```
   → 动画引擎挂树、`fx_parent`（浮字挂载点）已注入；`@Node2D@36/@Node2D@38` 经排查为**双方费用徽章的图集数字节点**
   （pos 82,90 / 1538,90，与左右六边形徽章位置一致）——已补入《当前状态》代码地图。

## 六、结论

**检查点10 通过（限定口径）** ✅ —— 真实鼠标「点目标 → 确认」攻击链完整走通，
伤害/反击/预览/按钮/日志/顶栏全部符合 a500 预期，两张截图 + 运行态读数留证；
**抖动中间帧**明确标注为"现有工具不可靠捕获"，由迭代004 既有证据承担（不重复声称）。
