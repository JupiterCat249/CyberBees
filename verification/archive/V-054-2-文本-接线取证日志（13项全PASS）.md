# 迭代054 接线取证日志（验证用 · 非生产）

> 状态：已封存（验证用，非生产文件）
> 对应迭代检查点：迭代054 · C4（UI 接线验证）
> 封存日期：2026-09-17
> 执行场景：`verification/iter054_wiring_check.tscn`

## 检查结果（13 项，全 PASS，失败项 = 0）

```
[PASS] 容器 Units 存在
[PASS] 容器 HandRight 存在
[PASS] 手牌卡实例数 = 4
[PASS] 单位卡实例数 = 2
[PASS] 手牌卡带控制器脚本
[PASS] 同名效果不叠加（层数=1）              ← T14
[PASS] 攻击力含效果修正 = 6                  ← 5 基础 + 1 效果，乘优先于加
[PASS] 运行态扣血不污染数据层                ← 设计案 R1/R2
[PASS] 手牌信号到达外层
[PASS] 单位信号到达外层
[PASS] 主按钮信号到达外层
[PASS] 可出牌态已作用到节点
[PASS] 详情区卡名已绑定
```

## 信号回路实测（子组件 → 控制器 → 外层）

```
外层收到 hand_card_clicked(46cf1b69-1633-4bdf-934e-c3985e0a4d90)
外层收到 unit_clicked(test)
外层收到 main_button_pressed
```

> 证明 D-1「靠信号互相通信而非直接互相引用」成立：
> 外层只 connect **控制器** 的信号；控制器把子组件信号**转发**；子组件**不认识**控制器。

## 验证中捕获并修复的 2 个真实缺陷

| # | 现象 | 真因 | 修法 |
|---|---|---|---|
| 1 | `card_hand.gd:37` 崩：`Cannot assign to 'text' on Nil` | **`@onready` 赋值时机**：`@onready` 变量在 `_ready()` **结束之后**才赋值，而父节点在 `add_child()` 时会同步调 `bind()`（含 `set_playable()`）→ 变量还是 null | 改**惰性取节点**（`_img()` / `_badge()` / `_body_panel()` 等方法内取） |
| 2 | `battle_scene.gd:188` 崩：`Invalid access to 'dmg' on Nil` | 把**非指令卡**硬转 `CommandData` → `as` 返回 null 后取字段 | 类型分支改为 `u != null / c != null` 判定，兜底显示 `-` |

## 截图
`V-054-1-图-新战斗场景接线取证（数据驱动渲染）.png`
