# 迭代059 步1 取证：X 费测试卡 + 规则验收（验证用 · 非生产）

> 状态：已封存（验证用，非生产文件）
> 对应迭代：迭代059 · 步1（地形体系与场地效果）
> 封存日期：2026-09-19

## 一、X 费测试卡（G-7 · 人指示「按往期惯例生成测试内容并在完成测试后封存」）

| 项 | 值 |
|---|---|
| 资源 | `test_x_cost_card.tres`（本目录同名副本即封存件） |
| 生成位置 | `verification/`（**不入 `game_data/` 生产目录**） |
| 类型 | `CommandData` · `cost = -1`（X 费） · `x_cost_multiplier = 2` · `target_range = 9` |
| 用途 | 跑通「X 费 = 目标部署费 × 倍率、不可对蜂王」 |

**测试结果**：X 费伤害 = 4（泥蜂部署费）× 2 = **8** ✅ · **不可对蜂王** ✅ · `x_cost_of(蜂王) = -1` ✅

## 二、规则验收（`verification/iter059_rules_check.gd`）：**44 / 0 全 PASS**

| 组 | 项数 | 覆盖 |
|---|---|---|
| G-1 地形格数据 | 15 | 3 张图坐标与人给值逐格一致 · 3 张图确认无地形格 · **3 张图地形格均关于棋盘中心中心对称** |
| G-3 铁锈 | 3 | 地形格单位获得力场 · 非地形格不给 |
| G-4 禁区 | 7 | 禁格不在部署预览 · 引擎查询接口 · **blocks_move/blocks_attack 均为 false（a500：不阻挡移动与攻击）** |
| G-5 水没 | 3 | 地形格 6 伤 → 4 · 非地形格 6 伤 → 6 · 不传地图数据时旧行为不变 |
| G-2 丰饶 | 4 | 第 2 回合不回费 · 第 3/9 回合 +4 · 不超上限 10 |
| 寒潮（回归） | 3 | 非生效回合不掉血 · 第 3 回合掉 2 · 蜂王免疫 |
| G-7 X 费 | 5 | 生成/回读 · 伤害 = 部署费 × 2 · 不可对蜂王 |
| G-8 同效果去重 | 4 | 两次装甲 UUID 不同（偏差根因实录）· **同名不加层** · 不同效果仍可并存 |

## 三、人给地形坐标的转换记录

人以**整张地图左上角**为原点给出 **(行, 列)**（1-based）；内部约定 `Vector2i(行, 列)`（0-based）→ **减 1**：

| 地图 | 人给（1-based） | 内部（0-based） | 中心对称校验 |
|---|---|---|---|
| 铁锈 | (2,4) · (3,1) | **(1,3) · (2,0)** | 偏移 `(-0.5,+1.5)` / `(+0.5,-1.5)` → 互为相反数 ✅ |
| 禁区 | (2,3) · (3,2) | **(1,2) · (2,1)** | 偏移 `(-0.5,+0.5)` / `(+0.5,-0.5)` → 互为相反数 ✅ |
| 水没 | (1,4) · (4,1) | **(0,3) · (3,0)** | 偏移 `(-1.5,+1.5)` / `(+1.5,-1.5)` → 互为相反数 ✅ |

> ⭐ 三张图的地形格**都恰好关于棋盘几何中心 (1.5,1.5) 中心对称**，与蜂王位置设计口径一致。

## 四、新增/改动文件

| 文件 | 说明 |
|---|---|
| `scripts/data/terrain_effect.gd`（新） | `TerrainEffect` 资源：语义（GRANT_FIELD/BLOCK_DEPLOY/COMMAND_REDUCE）+ 表现（图标/叠加色/文案） |
| `scripts/data/terrain_params.gd`（新） | `TerrainParams` 资源：地形格渲染参数（叠加色/描边/图标尺寸/悬停提示） |
| `scripts/data/map_data.gd` | 新增 `terrain_cells` + `terrain_effect` + `is_terrain_cell()` / `effect_at()` |
| `scripts/battle/rules_effects.gd` | 丰饶回费 · 铁锈地形力场 · **同效果改按名称去重（G-8）** |
| `scripts/battle/rules_combat.gd` | 指令伤害支持地形减免（G-5） |
| `scripts/battle/rules_preview.gd` | 部署预览排除禁格（预览与实际同源） |
| `scripts/battle/battle_engine.gd` | 部署校验拒绝禁格 + `terrain_blocks_deploy()` 查询接口 |
| `scripts/data/card_pool.gd` | `field_effect()` 力场工厂（grants_field + blocks_command，T14） |
| `verification/gen_resources.gd` | 逐图写入地形格 + 地形效果（**效果必须先存独立 .tres 才能挂上**） |
