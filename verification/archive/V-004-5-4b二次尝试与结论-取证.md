# V-004-5 迭代004 · 第4b步（二次尝试）与**结论** · 取证

> 状态：已封存（验证用，非生产文件）
> 对应迭代·检查点：迭代004 · 第4b步（表现时机接入）二次尝试
> 封存日期：2026-08-27

## 本轮改进（已落地、编译通过、**当前休眠**）

按上一轮定下的 4 条改进，对 `battle_anim.gd` 的 `bind()` 做了修正：

| 改进 | 内容 |
|------|------|
| ① 不用重绘信号驱动 | **移除** `state.state_changed` 连接（该信号每次 `refresh()` 都发，频率高）|
| ② 换低频语义信号 | 卡牌登场改由 **`state.turn_started`**（每回合一次）驱动探测 |
| ③ 只接必要信号 | `bind()` 现只接：`combat.attack_resolved`（受击）· `state.battle_ended`（终止）· `state.turn_started`（登场）|
| ④ 废弃入口保留但空实现 | `_on_state_changed()` 改为 `pass` + 注释说明废弃原因 |

协调器侧注入（`node_provider` 回调 + `an.bind(state)`）**已写入且编译通过** ✅

## 但验证阶段受阻（如实记录）

- 第 1 次：带 `while s.phase != 3: s.advance_phase()` 的 eval → **`EVAL_HUNG`** ✗
- 第 2 次：同结构（已移除重绘信号驱动后）→ **仍 `EVAL_HUNG`** ✗
- 第 3 次：`project_run` 的 MCP 请求**超时** ✗

**判断**：挂住的直接嫌疑是**验证脚本自身的推进循环**（若 `advance_phase()` 停在"换牌/弃卡/待确认"分支，
`phase` 不会前进 → `while` 空转 ✗），而**不是接线代码**（注入块编译无诊断）。
但**未能取得证据**证明接线在运行时无误 —— 故按纪律**回退接线**，保持确定可运行状态。

## 处置

`git checkout HEAD -- scenes/battle_flow.gd` → 回到 `4770f2a`（动画注入在位、无 `bind` 调用）✅
- 保留：引擎与 `bind()` 的**改进版**（编译通过、无人调用 → **行为零变化** ✅）
- 回退：注入回调与 `bind()` 调用

## 下一轮的正确验证方式（关键结论）

**不要再用"脚本推进游戏状态"的方式验证动画接入** ✗ —— 改用：
1. **真实鼠标 + 正常游戏流程**（`input_mouse`，与 `V-003.1-29` 同法）：
   点手牌→部署→进行动→点敌人→**观察受击抖动**（截图/像素取证）
2. 验证脚本中**禁止出现状态推进循环**；只做"读当前状态"（零副作用）
3. `project_run` 超时时先 `stop()` 再 `run()`，并留出启动时间

## 当前状态（可运行）

| 项 | 状态 |
|----|------|
| 引擎（306 行）+ 7 Pattern + 颜色/字号 + `bind()` 改进版 | ✅（休眠） |
| T3 残留清理 | ✅ |
| `state.anim` 注入（自检 17 项） | ✅ |
| 表现时机接入（4b） | ⏳ 未完成（机制就绪，**验证方式需改为真实鼠标**） |
| 工作区 | 干净 ✅ |
