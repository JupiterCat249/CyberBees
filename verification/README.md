# verification/ —— 验证产物专区（非生产文件）

> 状态：**验证用目录，不属于生产/工程文件**
> 建立：2026-08-27（迭代003.1 起，人明确要求）
> 最后更新：2026-08-27

## 为什么存在

迭代过程中会产生大量**只用于验证**的内容（临时验证脚本、构造的测试单位/卡牌、
eval 取证脚本、截图与日志片段）。这些内容若混在 `scenes/` `assets/` 等生产目录中，
会与正式生产文件混淆、污染工程结构。

## 约定

1. **目录**：本目录（游戏仓根 `verification/`）为验证产物**唯一存放处**；
   已完成验证的产物放入 `verification/archive/`。
2. **命名**：`V-<迭代>-<检查点>-<简述>.<ext>`，例如
   `V-003.1-6-单位过滤-取证.md`、`V-003.1-6-过滤验证.gd`。
3. **标记**（每个文件头部必须标注）：
   ```
   > 状态：已封存（验证用，非生产文件）
   > 对应迭代/检查点：
   > 封存日期：YYYY-MM-DD
   ```
4. **隔离铁律**：生产代码**禁止**引用本目录（不得出现 `preload("res://verification/...")`
   或 `load("res://verification/...")`）；验证脚本可引用生产代码，反向不可。
5. **验证数据**：构造的测试单位/卡牌一律运行时生成，**不写入**生产场景或资源文件。
6. **封存时机**：每个检查点验证通过后**立即**封存并标记，纳入该检查点的 commit。

## 与工程导出的关系

本目录内容**不参与游戏导出**（Godot 导出时按过滤器排除）；仅作可追溯的验证记录。

## 变更记录

| 日期 | 说明 |
|------|------|
| 2026-08-27 | 建档：迭代003.1 起启用（人要求：验证内容完成验证后封存并标记，独立目录避免与生产文件混淆） |

## ⚠️ 重要：必须保留 `.gdignore`

`verification/` 下**必须存在 `.gdignore` 文件**（Godot 规范：含该文件的目录会被引擎**完全忽略**）。

**原因（实测教训）**：归档在 `verification/archive/` 里的验证脚本若以 `.gd` 结尾，
**Godot 会把它当作项目脚本尝试加载** → 因文件头部带 Markdown 标记而 **Parse Error** ✗ →
**直接把运行中的游戏挂在调试断点上** ✗（本迭代实际发生）。

**规则**：
1. **禁止**在 `verification/` 下存放 `.gd` 文件（存档脚本一律用 `.gd.txt` 等非脚本后缀）；
2. 不得删除 `verification/.gdignore`；
3. 该目录因此天然不参与游戏导出，符合 T13 的隔离要求。
